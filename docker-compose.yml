version: '3.8'

services:
  jexi-os:
    build: .
    container_name: jexi-os
    ports:
      - "7860:7860"
    environment:
      - PORT=7860
      - DATA_DIR=/data
      - WORKSPACE_DIR=/data/workspace
    volumes:
      - jexi-data:/data
    restart: unless-stopped

volumes:
  jexi-data:
