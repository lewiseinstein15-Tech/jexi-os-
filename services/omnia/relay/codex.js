/**
 * JEXI OS — Phase 15 Scope B — Codex adapter.
 *
 * Same shape as any other adapter — registration, not hardcoding,
 * is what makes an agent known to the relay.
 */
export const codex = {
  name: 'codex',
  kind: 'coding-agent',
  capabilities: ['run', 'review'],
};
